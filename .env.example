# RHOKP environment variables.
#
# Copy this file to .env and fill in your values:
#   cp .env.example .env
#
# .env is gitignored and will NOT be committed.
# For OpenShift deployments, use 'oc create secret' instead — see deploy/README.md.

# ─── OKP container ───────────────────────────────────────────────────────────
# Decryption key for OKP encrypted content.  Required when running the
# rhokp container (podman-compose or OpenShift).
ACCESS_KEY=

# ─── OKP client settings ─────────────────────────────────────────────────────
# Base URL of your OKP instance (e.g. Podman or in-cluster).
RHOKP_BASE_URL=http://127.0.0.1:8080

# Solr request handler path.  Use /solr/portal/select (default) for general
# search or /solr/portal/select-errata for errata-specific field boosting.
# RHOKP_SOLR_HANDLER=/solr/portal/select

# Max number of doc snippets to retrieve (optional).
# RHOKP_RAG_ROWS=5

# ─── ADK agent / Llama Stack ─────────────────────────────────────────────────
# Base URL of your Llama Stack inference endpoint.  Required for the ADK agent.
LLAMA_STACK_BASE_URL=

# Model identifier from GET /v1/models (optional).
# MODEL=gemini/models/gemini-2.5-pro

# ─── OKP container tuning (optional) ─────────────────────────────────────────
# Memory allocated to Solr inside the OKP container.
# SOLR_MEM=1g
