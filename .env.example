# Red Hat Offline Knowledge Portal (OKP)
# Base URL of your OKP instance (e.g. Podman or in-cluster).
RHOKP_BASE_URL=http://127.0.0.1:8080
# Max number of doc snippets to retrieve (optional).
# RHOKP_RAG_ROWS=5

# Llama Stack (for demo: Responses API / Agents API)
# Base URL of your Llama Stack instance. Do not commit real URLs or tokens.
LLAMA_STACK_BASE=
# Model identifier from GET /v1/models (optional; script has a default).
# MODEL=gemini/models/gemini-2.5-flash
# Demo: pass tools (e.g. web_search) to Responses API; set to 0 to disable.
# RHOKP_USE_TOOLS=1

# OKP container: access key is required for encrypted content.
# Set only when running the container (e.g. podman run -e ACCESS_KEY=...).
# Do not put the key in this file if you commit .env.example; use .env (gitignored).
# ACCESS_KEY=
