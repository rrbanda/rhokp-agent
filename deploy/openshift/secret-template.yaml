# TEMPLATE ONLY -- do not put real secrets in this file.
#
# Create the actual secret with:
#   oc create secret generic rhokp-secrets \
#     --from-literal=ACCESS_KEY=<your_okp_access_key> \
#     --from-literal=LLAMA_STACK_BASE_URL=https://<your_llama_stack_endpoint>
#
# Or apply this template and patch afterwards:
#   oc apply -f secret-template.yaml
#   oc patch secret rhokp-secrets -p '{"stringData":{"ACCESS_KEY":"real-key"}}'

apiVersion: v1
kind: Secret
metadata:
  name: rhokp-secrets
  labels:
    app.kubernetes.io/part-of: rhokp
type: Opaque
stringData:
  ACCESS_KEY: ""
  LLAMA_STACK_BASE_URL: ""
